import React from 'react';
import ContentList from './ContentList';

const TestContentList = ({ title, images }) => {
  return (
    <ContentList title={title} images={images} />
  );
};

export default TestContentList;
